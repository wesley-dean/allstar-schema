{
  "$id": "https://json.schemastore.org/allstar.binary.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Allstar Binary Artifacts policy configuration",
  "type": "object",
  "definitions": {
    "OrgConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "type": "object",
          "description": "Standard org-level opt-in or opt-out configuration. RepoOverride applies to all configuration."
        },
        "action": {
          "description": "Which action to take. Default log; other options include issue.",
          "type": "string",
          "default": "log"
        },
        "ignoreFiles": {
          "type": "array",
          "description": "List of file names to ignore. Binary artifacts with these names are allowed. File name only, not a path. Globs are not allowed.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "optConfig",
        "action"
      ],
      "additionalProperties": false
    },
    "RepoConfig": {
      "type": "object",
      "properties": {
        "optConfig": {
          "type": "object",
          "description": "Standard repo-level opt-in or opt-out configuration."
        },
        "action": {
          "description": "Override for org-level action when present",
          "type": "string"
        },
        "ignorePaths": {
          "type": "array",
          "description": "List of full paths to ignore. If a binary artifact is reported at these paths, it is ignored. Must be full paths with directories. Globs are not allowed. Allowed even if repo override is disabled.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  },
  "anyOf": [
    {
      "$ref": "#/definitions/OrgConfig"
    },
    {
      "$ref": "#/definitions/RepoConfig"
    }
  ]
}
